# Prisma reads this to connect for migrate/generate/studio (db: scripts run via
# `node --env-file=.env`). Defaults match the postgres service in the repo-root
# docker-compose.yml, so this works immediately after `docker compose up -d`.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/restaurant
