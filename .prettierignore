# Turborepo cache (at root)
.turbo/

# Lock file — reformatting breaks pnpm's hash validation
pnpm-lock.yaml

# Generated & build output — never reformat
**/src/generated/**
**/dist/**
**/.next/**

# Agent-managed / vendored trees — kept intact (not auto-formatted)
.specify/
.claude/
.agents/

# SpecKit feature artifacts (spec/plan/tasks/research/etc.) — authored, not auto-formatted
specs/

# Agent guidance doc — authored, not auto-formatted
CLAUDE.md
